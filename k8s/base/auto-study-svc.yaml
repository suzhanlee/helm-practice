apiVersion: v1
kind: Service
metadata:
  name: auto-study-app-service
  namespace: app
spec:
  type: NodePort
  selector:
    app: auto-study-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
